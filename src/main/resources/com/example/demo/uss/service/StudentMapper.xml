<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.uss.service.StudentMapper">
   <insert id="insert" parameterType="com.example.demo.uss.service.Student">
       insert into students(userid, password, name, birthday, reg_date, profile_image, subject, gender)
		values (#{userid}, #{password}, #{name}, #{birthday}, #{regDate}, #{profileImage}, #{subject}, #{gender})
   </insert>
    <delete id="truncate" parameterType="hashmap">
    	${TRUNCATE_STUDENTS}
    </delete>
    <select id="count" resultType="int">
    	select count (*) as count from students 
    </select>
</mapper>